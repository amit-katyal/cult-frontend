export const metadata = {
    title: 'Create a New Token - The Cult',
    description: 'Create your own celebrity token and join The Cult community.',
  };
  
  export default function TokenCreateLayout({ children }) {
    return children;
  }